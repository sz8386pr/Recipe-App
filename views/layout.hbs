<!DOCTYPE html>
<html>
    <head>
        <title>Recipe App</title>
        <link rel="stylesheet" href="/stylesheets/sakura-earthly.css" type="text/css">
        <link rel='stylesheet' href='/stylesheets/style.css' />
      </head>
      <body>

    {{#if user}}
        <p style="text-align: right">Welcome {{user.username}} <a href="/auth/logout">Logout</a></p>

    {{else}}
        <p style="text-align: right"><a href="/auth/login">Login</a> <a href="/auth/signup">Sign up</a></p>

    {{/if}}
    <hr>
      {{{ body }}}
      </body>
</html>
